{
  "name": "destinyations",
  "version": "1.0.0",
  "description": "Travel wishlist SPA",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
